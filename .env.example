# =============================================================================
# Talent Reconnect Agent - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Configuration (Required)
# -----------------------------------------------------------------------------
# Option 1: Azure OpenAI
AZURE_OPENAI_ENDPOINT=https://your-openai.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini

# Option 2: Compass API (G42)
# COMPASS_API_KEY=your-compass-key
# COMPASS_BASE_URL=https://api.core42.ai/v1
# COMPASS_MODEL=gpt-4.1

# -----------------------------------------------------------------------------
# Azure AI Search - Resume Database (100k+ CVs)
# Region: UAE North (PII compliant)
# -----------------------------------------------------------------------------
# Set to "false" to use Azure AI Search, "true" for demo mock data
USE_MOCK_DATA=true

# Azure AI Search connection
AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_KEY=your-search-admin-key
AZURE_SEARCH_INDEX=resumes

# Search configuration
USE_SEMANTIC_SEARCH=false
SEMANTIC_CONFIG_NAME=resume-semantic-config
VECTOR_FIELD_NAME=embedding

# Optional: Azure OpenAI for embeddings (only if using vector search)
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small

# -----------------------------------------------------------------------------
# Index Schema Reference
# -----------------------------------------------------------------------------
# The Azure AI Search index should have these fields:
#
# Required fields:
#   - id (string, key)
#   - name (string, searchable)
#   - email (string)
#   - experience_years (int32, filterable)
#   - education (string, searchable)
#   - location (string, searchable, filterable)
#   - current_company (string, searchable)
#   - current_title (string, searchable)
#   - skills (Collection(string), searchable, filterable)
#   - summary (string, searchable)
#
# Optional fields:
#   - phone (string)
#   - certifications (Collection(string))
#   - resume_text (string, searchable) - full resume content
#   - ml_production (boolean, filterable)
#   - genai_experience (boolean, filterable)
#   - azure_experience (boolean, filterable)
#   - e2e_pipelines (boolean, filterable)
#   - open_to_opportunities (boolean, filterable)
#   - last_updated (DateTimeOffset)
#   - source (string)
#   - embedding (Collection(Single)) - for vector search
#
# =============================================================================
